[Unit]
Description=Redis persistent key-value database
After=network.target

[Service]
ExecStart=/usr/bin/redis-server {{config_file}} --daemonize no
ExecStop=/usr/bin/redis-shutdown
User={{user}}
Group={{group}}

[Install]
WantedBy=multi-user.target
